import React from 'react';

const LaborCostWidget: React.FC = () => {
  return <div className="p-4 text-xs text-slate-500">LaborCostWidget indisponivel neste build.</div>;
};

export default LaborCostWidget;
