import React from 'react';

const NegativeStockMonitorWidget: React.FC = () => {
  return <div className="p-4 text-xs text-slate-500">NegativeStockMonitorWidget indisponivel neste build.</div>;
};

export default NegativeStockMonitorWidget;
